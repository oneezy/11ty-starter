---
permalink: "./src/_opengraph/opengraph.json"
permalinkBypassOutputDir: true
eleventyExcludeFromCollections: true
---
[
{%- for opengraph in collections.all %}
  {
      "title":"{{ opengraph.data.title | safe }}",
      "imgName":"{{ opengraph.data.title | slugify }}"
  }{% if loop.last == false %},{% endif -%}
{% endfor %}
]